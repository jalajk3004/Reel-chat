
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['static.cdninstagram.com'], // Allow images from this domain
    },
  };
  
  export default nextConfig;
  